# Benchmark: Atlas Runtime end-to-end validation (Qwen2.5-0.5B-Instruct Q4_K_M)

Date: 2026-08-04

## Purpose

This is an **engineering validation** benchmark, not a competition
throughput claim. It exists to prove the full Runtime stack — GGUF
Inspector → Model Registry → Model Validation → Runtime Manager (spawns
`atlas-inference-worker`) → real llama.cpp FFI → IPC token streaming →
Benchmark Engine — works correctly end to end, with real measured
numbers, per `docs/engineering-standards.md`'s "no fake benchmarking"
standard.

**This is explicitly not the Africa Deep Tech Challenge reference
benchmark.** Per the project's Runtime Philosophy ("Atlas is not built
around a model") and explicit instruction, **Qwen 3 4B Instruct Q4_K_M
remains the official reference model and benchmark target.** A smaller
model was used here solely because it is fast and low-risk to validate
against in this specific development sandbox (a ~468MB download and a
smaller RAM/compute footprint than the ~2.5GB 4B reference model). See
§"Not yet done" below for what's still required before any Africa Deep
Tech Challenge–facing claim can be made.

## Methodology

**Hardware:**

| Field | Value |
|---|---|
| CPU | Intel(R) Core(TM) i5-8265U CPU @ 1.60GHz |
| Physical cores | 4 |
| Logical cores | 8 |
| Total memory | 8,135,716,864 bytes (7.58 GiB) |
| Available memory (at test time) | ~4.5–4.8 GiB |
| RAM tier selected | Standard (per ADR-0006's `select_tier`) |
| OS | Kali GNU/Linux Rolling (development sandbox — **not** the Ubuntu 22.04 competition reference OS; see caveats below) |

All hardware figures above are from `atlas_engine::inference::hardware::detect_hardware()`,
run live at benchmark time — not looked up from a spec sheet.

**Software:**

- BRIX Atlas commit: this session's work-in-progress (Runtime bootstrap,
  post ADR-0010)
- `llama-cpp-2` v0.1.153 / `llama-cpp-sys-2` v0.1.153 (llama.cpp compiled
  from source via `cmake`, CPU-only, `n_gpu_layers = 0`)
- Model: `Qwen/Qwen2.5-0.5B-Instruct-GGUF`,
  `qwen2.5-0.5b-instruct-q4_k_m.gguf` (official Qwen release, same
  quantization scheme — Q4_K_M — as the Africa Deep Tech Challenge
  reference model)
- Model file size: 491,400,032 bytes (468.5 MiB)

**Test:** single-turn prompt
`"<|im_start|>user\nWhat is the capital of Kenya?<|im_end|>\n<|im_start|>assistant\n"`,
`max_tokens = 64`, default sampling (`temperature = 0.7`, `top_p = 0.95`,
`top_k = 40`), run via `cargo run -p atlas-engine --example
validate_runtime` (`crates/atlas-engine/examples/validate_runtime.rs`).
Each run is a fresh process invocation: new `RuntimeManager`, fresh
worker spawn, fresh model load, fresh `LlamaContext` — i.e. every number
below includes cold-start cost, not just steady-state generation.

## Raw results (n = 4 independent runs)

| Run | Prompt tokens | Generated tokens | Tokens/sec | Total duration (ms) |
|---|---|---|---|---|
| 1 | 15 | 7 | 31.45 | 441 |
| 2 | 15 | 7 | 35.62 | 413 |
| 3 | 15 | 7 | 23.86 | 496 |
| 4 | 15 | 7 | 25.71 | 510 |

- **Tokens/sec:** min 23.86, max 35.62, mean ≈ 29.16
- **Total duration:** min 413ms, max 510ms, mean ≈ 465ms
- **Model load time** (measured separately, one sample): 868ms

**On sample size and percentiles:** `docs/benchmarks/README.md` asks for
p50/p95/p99 where latency is reported. With n = 4 and only 7 generated
tokens per run (the model reliably produces the exact correct short
answer, then an end-of-generation token), percentiles would imply a
statistical precision this dataset doesn't have — reporting them would
be exactly the kind of fabricated-precision this project's standards
prohibit. Min/mean/max is the honest summary at this sample size. A
larger-n, longer-generation, warm-vs-cold-state-controlled benchmark is
explicitly future work (see the remaining roadmap in
`docs/architecture/runtime-architecture.md`, §7), not something this
report pretends to already be.

**Generated output** (identical across all 4 runs): *"The capital of
Kenya is Nairobi."* — correct.

**Process-level measurements** (one sample): RSS 13.6 MB, CPU usage
5.03% at time of measurement (`atlas_engine::inference::metrics`, real
`sysinfo`-backed reads, not estimated).

## Interpretation

- The Runtime correctly detects this machine as Standard tier (7.58 GiB
  total memory clears the 7 GiB threshold in
  `docs/adr/0006-quantization-model-tiering-ram-envelope.md`) — a good
  sign the tier logic's real-world "OS-reported RAM is a bit under the
  nominal figure" handling (documented in
  `crates/atlas-engine/src/inference/memory.rs`) is correct in practice,
  not just in a synthetic unit test.
- Run-to-run variance (23.9–35.6 tok/s) is high in *relative* terms but
  the *absolute* range (≈12 tok/s) is consistent with what's expected
  from only 7 generated tokens per sample — the first token's latency
  (sampling setup, KV-cache warm-up) dominates a 7-token generation far
  more than it would a 200-token one. This is a sample-size artifact to
  fix in a follow-up benchmark, not evidence of an unstable Runtime.
- Cold-start-inclusive total duration (413–510ms) for a 0.5B model
  includes: process spawn, llama.cpp backend init, IPC connection
  establishment, tokenization, prompt decode, and generation. Model
  *loading* (868ms, measured separately) is not included in these
  per-request numbers — it happens once per worker lifetime, not once
  per request.

## Not yet done (required before an Africa Deep Tech Challenge–facing claim)

1. **Run this same methodology against the official Qwen 3 4B Instruct
   Q4_K_M reference model.** Not done in this sandbox by deliberate,
   documented choice (see §Purpose) — the 4B model's RAM footprint and
   download size were judged a higher risk to *this development
   session* than to a real target machine. This is the single most
   important remaining benchmark.
2. **Run on Ubuntu 22.04**, the actual competition reference OS — this
   sandbox runs Kali GNU/Linux Rolling. The Runtime's logic is
   OS-generic (no Kali-specific code path exists anywhere), but the
   number itself hasn't been measured on the reference OS.
3. **Larger sample size with longer generations** (e.g. n ≥ 20, ≥200
   tokens each) to get statistically meaningful percentile latency
   figures, warm-state (worker already running, model already loaded)
   separated from cold-start figures.
4. **Thermal stability under sustained load** — this report is a single
   short burst per run, not a sustained-load thermal test (one of the
   five competition optimization targets alongside accuracy, throughput,
   RAM efficiency, and UX).
